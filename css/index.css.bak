html {
    color-scheme: light;
}

html.dark {
    color-scheme: dark;
}

@media (prefers-reduced-motion: no-preference) {
    html {
        scroll-behavior: smooth;
    }
}

body {
    font-family: 'Inter', sans-serif;
}

.text-shadow {
    text-shadow: 1px 1px 5px rgb(0 0 0 / 70%);
}

.with-bg::before {
    content: '';
    position: absolute;
    inset: 0;
    background-color: #4A5568;
    background-image: url('img/ppt3.jpg');
    background-size: cover;
    background-position: center;
    filter: blur(2px) brightness(40%);
    z-index: -1;
    transform: scale(1.1); 
}

/* intro overlay is hidden unless scripting explicitly enables it */
#intro-overlay {
    display: none;
}

/* only shown once the head script confirms js is running */
html.intro-ready #intro-overlay {
    display: flex;
}

/* lock scroll via the same class so it can never get stuck */
html.intro-ready,
html.intro-ready body {
    overflow: hidden;
}

/* keyboard focus ring for the overlay */
#intro-overlay:focus-visible {
    outline: 2px solid #fdba74;
    outline-offset: -6px;
}

/* one knob for every card thumbnail: raise the first number to get shorter cards */
:root {
    --thumb-ratio: 2 / 1;
}

/* the box keeps a fixed shape, the image is never cropped inside it */
.thumb img {
    aspect-ratio: var(--thumb-ratio);
}

/* thumbnail: fill the letterbox with a blurred copy of the same image */
.thumb::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: var(--thumb-src);
    background-size: cover;
    background-position: center;
    filter: blur(24px) saturate(140%);
    transform: scale(1.25);
    opacity: 0.45;
    z-index: 0;
}

/* keep the backdrop subtle on dark surfaces */
html.dark .thumb::before {
    opacity: 0.3;
}

/* no backdrop until js resolves the source */
.thumb:not([style*="--thumb-src"])::before {
    display: none;
}

/* flash the target card when an award links into it */
@keyframes jumpFlash {
    0%   { box-shadow: 0 0 0 0 rgba(249, 115, 22, 0.55); }
    100% { box-shadow: 0 0 0 14px rgba(249, 115, 22, 0); }
}

/* ring pulses twice then settles */
.jump-target {
    animation: jumpFlash 1s ease-out 2;
    border-color: #f97316 !important;
}

/* reduced motion: hold a static ring instead of pulsing */
@media (prefers-reduced-motion: reduce) {
    .jump-target {
        animation: none;
        box-shadow: 0 0 0 3px rgba(249, 115, 22, 0.5);
    }
}

/* ongoing project: soft breathing glow around the card */
@keyframes ongoingGlow {
    0%, 100% { box-shadow: 0 0 0 0 rgba(16, 185, 129, 0); }
    50%      { box-shadow: 0 0 0 5px rgba(16, 185, 129, 0.18); }
}

/* slow pulse so it reads as active, not as an alert */
.ongoing-card {
    animation: ongoingGlow 3.2s ease-in-out infinite;
}

/* small status dot next to the date */
@keyframes liveDot {
    0%, 100% { opacity: 1; transform: scale(1); }
    50%      { opacity: 0.45; transform: scale(0.82); }
}

/* keeps ticking to signal work in progress */
.live-dot {
    animation: liveDot 1.8s ease-in-out infinite;
}

/* motion-sensitive users get the outline without the movement */
@media (prefers-reduced-motion: reduce) {
    .ongoing-card,
    .live-dot {
        animation: none;
    }
}

/* 3d perspective utilities */
.perspective-1000 {
    perspective: 1000px;
}

/* maintain 3d child elements */
.preserve-3d {
    transform-style: preserve-3d;
}

/* float text above card surface */
.translate-z-10 {
    transform: translateZ(40px);
}

/* initial drop-in animation for the card */
@keyframes dropIn {
    0% {
        opacity: 0;
        transform: translateY(-40px) scale(0.9);
    }
    100% {
        opacity: 1;
        transform: translateY(0) scale(1);
    }
}

/* apply drop animation on load */
.card-drop-in {
    animation: dropIn 0.8s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
}

/* zoom-in effect when entering the site */
.card-zoom-out {
    transform: scale(6) !important;
    opacity: 0 !important;
    transition: transform 0.8s cubic-bezier(0.7, 0, 0.3, 1), opacity 0.6s ease-in !important;
    pointer-events: none;
}

/* reduced-motion: keep the one-shot intro, drop only the parallax travel */
@media (prefers-reduced-motion: reduce) {
    @keyframes dropIn {
        0% {
            opacity: 0;
        }
        100% {
            opacity: 1;
        }
    }
}
